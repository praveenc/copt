# Rust build artifacts
/debug/
/target/
# Note: Cargo.lock is committed for binary projects (reproducible builds)

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Dev container configuration (uses shared docker-compose.yml outside repo)
.devcontainer/

# Beads (context snapshots)
.beads/

# Claude Code configuration
.claude/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Environment files
.env
.env.local
.env.*.local

# Test artifacts
*.log
*.tmp

### rust-analyzer ###
# Can be generated by other build systems other than cargo (ex: bazelbuild/rust_rules)
rust-project.json

# copt output directory (auto-generated optimized prompts)
/copt-output/

# temp directories
docs/tmp/
images/tmp/

# Test prompt files (if any)
test_prompt.txt

# bv (beads viewer) local config and caches
.bv/
.claude/*.local.*
# Git worktrees directory
/worktrees/
